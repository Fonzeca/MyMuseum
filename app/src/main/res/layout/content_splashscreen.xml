<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.a000webhostapp.mymuseum.Splashscreen"
    >

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/splashscreen_dark_small"
        android:scaleType="centerCrop"
        />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/splashscreen_title"
        android:layout_centerInParent="true"
        android:textColor="#FFF"
        android:textSize="36sp"
        android:id="@+id/splashscreen_title"
        android:textStyle="bold"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/splashscreen_version"
        android:layout_centerHorizontal="true"
        android:textColor="#FFF"
        android:layout_below="@id/splashscreen_title"
        android:textSize="20sp"
        />

</RelativeLayout>
